# Day 4 synthetic evaluation data

`seeded_artifact/order_service.py` is deliberately defective classroom code. It must
not be reused in an application. `golden_defects.json` is the instructor-owned answer
key used to measure recall and false positives. Students should inspect the artifact
before the answer key is revealed, then use the key for evaluation.
