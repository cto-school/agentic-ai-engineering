# Day 2 Course Data

The three Markdown documents are fictional teaching artifacts created for this course. They may be redistributed with the repository.

They are deliberately small so students can inspect every chunk and understand failures without a large ingestion pipeline.

`golden_set.json` contains 10 cases used to evaluate retrieval separately from answer generation. It must not be placed inside the indexed corpus.

